#include "localstorage.hpp"
#include "webconnector.hpp"
#include "wificlient.hpp"

#include <iostream>
#include <WiFi.h>
using namespace std;
/*
void setup() {
  LocalStorage storage;
  WebConnector webConnector(storage);
  //ConfigMQTT configMQTT;
  //Runner runner;
  //WifiClient wifiConnection;
  

  if (webConnector.deviceConfigured()) {
    storage.LoadData();
  } else {
    webConnector.configureServer();
  }
  
}

void resetDevice() {
  //Serial.println("DeviceReset");
  cout << "hi";
}

void loop() {
  //resetDevice();
  //delay(1000);
}
*/
int main(int argc, char const *argv[])
{
  /*
  LocalStorage storage;
  WebConnector webConnector(storage);
  WifiClient wifiClient;


  wifiClient.wifi = "hello";
  cout << "wifi: " << wifiClient.wifi << "\n";

  webConnector.tellWifiServerToSetX(3);
  int storageX = 1;
  cout << "storageX: " << storageX;
  storageX = webConnector.tellWifiServerToGetX();
  cout << "\nstorageX: " << storageX << "\n";
  return 0;*/
}
